services:
  alcoves:
    user: "${UID}:${GID}"
    command: >
      /bin/sh -c "uv run manage.py migrate && uv run manage.py runserver 0.0.0.0:8000"
    build: .
    volumes:
      - .:/app
      - ./data:/data
      - venv:/app/.venv
    ports:
      - 8000:8000

volumes:
  venv:

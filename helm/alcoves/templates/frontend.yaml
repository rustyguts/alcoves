{{- if .Values.frontend.enabled }}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "alcoves.frontend.fullname" . }}
  labels:
    {{- include "alcoves.labels" . | nindent 4 }}
    app.kubernetes.io/component: frontend
spec:
  replicas: {{ .Values.frontend.replicaCount }}
  selector:
    matchLabels:
      {{- include "alcoves.selectorLabels" . | nindent 6 }}
      app.kubernetes.io/component: frontend
  template:
    metadata:
      labels:
        {{- include "alcoves.selectorLabels" . | nindent 8 }}
        app.kubernetes.io/component: frontend
      {{- with .Values.frontend.podAnnotations }}
      annotations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
    spec:
      serviceAccountName: {{ include "alcoves.serviceAccountName" . }}
      {{- with .Values.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.podSecurityContext }}
      securityContext:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      containers:
        - name: frontend
          image: {{ include "alcoves.image" . }}
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          # Unified image, frontend role: run only the SvelteKit server.
          args: ["web"]
          {{- with .Values.securityContext }}
          securityContext:
            {{- toYaml . | nindent 12 }}
          {{- end }}
          ports:
            - name: http
              containerPort: {{ .Values.frontend.port }}
              protocol: TCP
          env:
            # adapter-node server (FRONTEND_* envPrefix — see svelte.config.js).
            - name: FRONTEND_HOST
              value: 0.0.0.0
            - name: FRONTEND_PORT
              value: "{{ .Values.frontend.port }}"
            # Derive the request origin from the ingress (no fixed ORIGIN needed).
            - name: FRONTEND_PROTOCOL_HEADER
              value: x-forwarded-proto
            - name: FRONTEND_HOST_HEADER
              value: x-forwarded-host
            # Unbounded so TUS chunk PATCHes streamed through the /api proxy aren't rejected.
            - name: FRONTEND_BODY_SIZE_LIMIT
              value: "Infinity"
            # SSR-side + /api proxy: hits the in-cluster API service.
            - name: INTERNAL_API_URL
              value: http://{{ include "alcoves.api.fullname" . }}:{{ .Values.backend.api.port }}
            # Client-side: browsers reach the API directly so video, image proxy,
            # downloads + the activity WebSocket bypass the SvelteKit /api proxy
            # (avoids range-buffer mangling and offloads streaming).
            - name: PUBLIC_API_ORIGIN
              value: {{ include "alcoves.publicApiOrigin" . | quote }}
          livenessProbe:
            httpGet: { path: /, port: http }
            initialDelaySeconds: 20
            periodSeconds: 30
          readinessProbe:
            httpGet: { path: /, port: http }
            initialDelaySeconds: 5
            periodSeconds: 5
          resources:
            {{- toYaml .Values.frontend.resources | nindent 12 }}
      {{- with .Values.frontend.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.frontend.affinity }}
      affinity:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.frontend.tolerations }}
      tolerations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
---
apiVersion: v1
kind: Service
metadata:
  name: {{ include "alcoves.frontend.fullname" . }}
  labels:
    {{- include "alcoves.labels" . | nindent 4 }}
    app.kubernetes.io/component: frontend
  {{- with .Values.service.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - name: http
      port: {{ .Values.frontend.port }}
      targetPort: http
      protocol: TCP
  selector:
    {{- include "alcoves.selectorLabels" . | nindent 4 }}
    app.kubernetes.io/component: frontend
{{- end }}

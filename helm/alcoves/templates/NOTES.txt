Alcoves {{ .Chart.AppVersion }} installed as {{ .Release.Name }}.

Public URL:
  {{ .Values.baseUrl }}

Components deployed:
  frontend (SvelteKit SSR): {{ .Values.frontend.replicaCount }} replica(s)
{{- if .Values.backend.api.enabled }}
  api:                 {{ .Values.backend.api.replicaCount }} replica(s)
{{- end }}
{{- if .Values.backend.worker.enabled }}
  worker:              {{ .Values.backend.worker.replicaCount }} replica(s)
{{- end }}

Storage driver: {{ .Values.storage.driver }}
Database URL:   set via secret
Queue:          {{ .Values.queue.host }}:{{ .Values.queue.port }}

Verify:
  kubectl --namespace {{ .Release.Namespace }} get pods -l app.kubernetes.io/instance={{ .Release.Name }}

Tail logs:
  kubectl --namespace {{ .Release.Namespace }} logs -l app.kubernetes.io/component=backend-api -f

Worker (transcribe / audio detect / video proxy) logs:
  kubectl --namespace {{ .Release.Namespace }} logs -l app.kubernetes.io/component=backend-worker -f

If using local storage driver, the data PVC is:
  {{ default (printf "%s-data" (include "alcoves.fullname" .)) .Values.storage.persistentVolume.existingClaim }}

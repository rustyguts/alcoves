apiVersion: v2
name: alcoves
description: Alcoves — self-hosted collaborative file library (Nuxt frontend + Go API + async workers)
type: application
# `version` is the chart version, `appVersion` is the running app version.
# Both mirror /VERSION at the repo root and are bumped automatically by
# release-please via the trailing # x-release-please-version annotation.
version: 0.18.0 # x-release-please-version
appVersion: "0.18.0" # x-release-please-version
home: https://github.com/yourorg/alcoves
keywords:
  - media
  - library
  - video
  - photos
  - selfhosted
maintainers:
  - name: Alcoves Maintainers

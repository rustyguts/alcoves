apiVersion: v2
name: alcoves
description: >-
  Alcoves — self-hosted collaborative file library (Nuxt frontend + Go API +
  async workers)
type: application
# `version` is the chart version, `appVersion` is the running app version.
# Both mirror /VERSION at the repo root — keep them in sync when bumping.
version: 0.16.1
appVersion: "0.16.1"
home: https://github.com/yourorg/alcoves
keywords:
  - media
  - library
  - video
  - photos
  - selfhosted
maintainers:
  - name: Alcoves Maintainers

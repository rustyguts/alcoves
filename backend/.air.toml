root = "."
tmp_dir = "tmp"

[build]
  cmd = "go build -tags dev -o ./tmp/main ./cmd/server"
  bin = "./tmp/main"
  include_ext = ["go", "toml"]
  exclude_dir = ["tmp", "vendor", "data", "migrations"]
  delay = 1000

[misc]
  clean_on_exit = true
